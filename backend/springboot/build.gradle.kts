// Root project — intentionally empty.
// Per ADR-01 v2: common build logic lives in buildSrc/ convention plugins
// (playground.java-conventions, playground.spring-boot-app, and per-BC
// layer plugins added when M1 lands).
